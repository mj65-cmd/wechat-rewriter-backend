# 后端依赖（轻量版 v3.2：适配 Render 免费 512MB 实例，任意 Python 版本可秒级构建）
# 抓取走 jina.ai Reader（免密钥），ASR 走前端浏览器 Whisper，故无需 trafilatura/yt-dlp/sherpa-onnx
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
httpx==0.27.2
openai==1.51.0
requests==2.32.3
